---
title: マルチエージェント TRPG アーキテクチャ検討メモ
date: 2026-03-27
updated: 2026-03-29 (2)
---

# マルチエージェント TRPG — アーキテクチャ比較

## 確定した制約

- サブエージェントからサブエージェントは起動できない
- spawn できるのはメインチャットのみ
- 「GM がオーケストレーターを兼任して PC を spawn する」A案の実装は不可能

---

## 案1：メイン = オーケストレーター、GM・PC は全員サブエージェント（逐次）

```
メインチャット（オーケストレーター）
 ├─ GM を spawn → シーン描写・裁定を受け取る
 ├─ PC1 を spawn（公開情報 + PC1 秘匿情報）→ 内心・発言・行動を受け取る
 ├─ PC2 を spawn（公開情報 + PC2 秘匿情報）→ 内心・発言・行動を受け取る
 └─ PC3 を spawn（公開情報 + PC3 秘匿情報）→ 内心・発言・行動を受け取る
```

**利点**
- 実装が最も単純
- 情報管理がメイン一箇所に集まる
- 秘匿情報の保証が確実

**欠点**
- GM もステートレスになる（毎回セッション履歴を渡す必要がある）
- PC が逐次起動のため「同時に考える」感覚が出ない
- 実質 B案（中立オーケストレーター＋全員サブエージェント）と同型

---

## 案2：メインでオーケストレーション、PC のみ並列 spawn（初回採用・問題あり）

```
メインチャット
 Step1: GM を spawn → シーン描写を受け取る
 Step2: PC1・PC2・PC3 を同時並列 spawn（同じシーン描写を渡す）
         → 三者の内心・発言を同時に受け取る
 Step3: GM を spawn（PC の行動宣言を渡す）→ 裁定・結果描写を受け取る
 → Step1 に戻る（次シーンへ）
```

協議フェーズが必要な場面では追加 spawn を挟む：

```
Step2a: PC1 を spawn → 発言を受け取る
Step2b: PC1 の発言を渡して PC2・PC3 を並列 spawn → 反応を受け取る
Step2c: 全員の発言を渡して行動宣言 spawn
```

**利点**
- PC たちの「同時に考える」という TRPG の本質を再現できる
- 1ラウンドの所要 spawn 数が最小（2回＋並列1回）
- 協議が深まる場面だけ追加 spawn で柔軟に対応可能

**欠点**
- GM がステートレス（毎回セッション要約を渡す必要がある）
- PC が互いの発言を「聞いてから反応する」協議の流れが一工夫必要

**実運用で判明した問題（session_log_01 より）**

案2でセッションを実施した結果、以下の構造的問題が顕在化した：

- GM へ渡すセッション要約の精度に一貫性が依存するため、要約の省略・誤りが
  そのまま物語の矛盾として現れる（ゼーンの所在矛盾、装備残骸の場所矛盾など）
- 「誰が何を知っているか」の管理がオーケストレーターの手作業になり、
  秘匿情報が公開された後も秘匿として残るケースが発生した
- オーケストレーターが「情報の仕分け・配送係」を担う設計は、
  実際のTRPGテーブル（全発言が全員に自動的に届く）とかけ離れており、
  転送漏れ・転送誤りが一貫性欠如の根本原因となった

---

## 案3：GM のみ永続バックグラウンド、PC はサブエージェント

```
メインチャット
 ├─ GM をバックグラウンド起動（永続・SendMessage で通信）
 │   → セッション全体を通じて文脈を保持し続ける
 │
 ├─ PC1 を spawn → 応答を GM に転送（SendMessage）
 ├─ PC2 を spawn → 応答を GM に転送
 └─ PC3 を spawn → 応答を GM に転送
```

**利点**
- GM が真に「セッションを覚えている」存在になる
- 毎回履歴を渡す必要がない
- TRPG の GM が「同じセッションを通して記憶を持つ」という本質に最も近い

**欠点**
- GM のコンテキストウィンドウがセッションを通じて増大する
- GM への SendMessage の調整コストをメインチャットが負担する
- GM が応答を返すタイミングの同期が必要

---

## 案4：「シーン単位バッチ」— GM にシナリオ進行を委ねる

```
メインチャット
 ├─ GM を spawn：「次の1シーンを進めてください。
 │   PC の行動が必要な箇所では【PC1待ち：○○】と記して止めてください」
 │   → GM が描写を書き、PC へのキューを出力して止まる
 │
 ├─ GM の出力からキューを読み取り、該当 PC を spawn
 └─ PC の応答を持って GM を再 spawn → 続きを書かせる
```

**利点**
- GM のペース感・物語判断を活かせる
- メインの介入が少なくて済む
- シーンの自然な流れを GM が主導できる

**欠点**
- GM が「どこで止まるか」を正確に記述してくれるかの信頼性に依存する
- GM がキューを書き忘れると止まるべきところで止まらない

---

## 案5：公開ログ共有 ＋ 全エージェント永続 ★改訂推奨★

session_log_01 の実運用と事後分析を踏まえた改訂案。

### 設計の核心：TRPGテーブルの情報構造を忠実に写す

実際のTRPGテーブルでは、情報は2種類しかない：

```
声に出したこと（全員に届く）：
  GM の描写・NPC 発言、PC の発言・行動宣言、ルール確認・メタ発言

頭の中にあること（本人のみ、または紙で GM に渡す）：
  PC の内心、GM 未公開のシナリオ情報、GM から特定 PC へ渡す秘密メモ
```

この構造をそのまま実装する：

```
┌──────────────────────────────────────────────┐
│  公開ログ  session_log.md                     │
│  オーケストレーターのみが書く                  │
│  全エージェントが読む（差分で受け取る）         │
└──────────────────────────────────────────────┘

┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│GM 秘匿   │  │PC1 内心  │  │PC2 内心  │  │PC3 内心  │
│未公開情報│  │本人のみ  │  │本人のみ  │  │本人のみ  │
└──────────┘  └──────────┘  └──────────┘  └──────────┘
```

### エージェントの起動方式：全員を永続バックグラウンドで起動

```
セッション開始時：
  GM  → run_in_background: true, name: "GM"  で起動
  PC1 → run_in_background: true, name: "PC1" で起動
  PC2 → run_in_background: true, name: "PC2" で起動
  PC3 → run_in_background: true, name: "PC3" で起動

以降はすべて SendMessage で通信。
エージェントは蓄積コンテキストにより「自分の知っていること」を保持し続ける。
```

### ターン進行フロー

```
【GMターン】
  SendMessage → GM
    「公開ログ差分：[前回から追加された内容]
     指示：次のシーンを描写してください」
  GM が返答する（public 部分のみ）
  オーケストレーター → 公開ログに追記

【PC並列ターン】
  SendMessage → PC1・PC2・PC3（同時）
    「公開ログ差分：[GMの描写]
     指示：発言・行動を返してください」
  各 PC が返答する：
    { "public": "発言・行動", "inner": "（内心：……）" }
  オーケストレーター → public を公開ログに順次追記
  オーケストレーター → inner をセッション記録に書くが他エージェントには送らない

【GM裁定ターン】
  SendMessage → GM
    「公開ログ差分：[PC たちの行動]
     指示：判定・結果描写をしてください」
  → GMターンへ戻る
```

### オーケストレーターの責務（簡略化）

| やること | やらないこと |
|----------|-------------|
| ターン順序の管理 | 内容の要約・再解釈 |
| 各エージェントに差分ログ＋指示を送る | 矛盾の検出・修正 |
| 返答の public 部分を公開ログへ追記 | 「何を誰に届けるか」の細かい判断 |
| 返答の inner 部分を記録用ファイルへ書く | |
| 秘匿情報を特定エージェントのみに送る | |

「情報の仕分け・配送係」から「書記＋指揮者」へ役割を絞ることで、
転送漏れ・転送誤りによる一貫性欠如を構造的に防ぐ。

### 秘匿情報の扱い

```
GM が特定 PC にのみ情報を渡したいとき：
  SendMessage → PC1 のみ
    「公開ログ差分：[通常差分]
     秘匿情報（あなたのみ）：[内容]
     指示：…」

→ PC2・PC3 への SendMessage には秘匿情報を含めない
→ 「渡さない」だけで秘匿が実現する（案2 と同様の原則）
→ 公開された瞬間（PC が public として発言した瞬間）に
  オーケストレーターが公開ログへ書くことで自動的に全員に届く
```

### 内心の設計方針

内心は実際のTRPGには存在しない「小説的拡張」であり、3つの設計方針がある：

| 方針 | GM に見えるか | ゲームへの影響 | 物語としての読み応え |
|------|-------------|--------------|-------------------|
| A：GM だけが読む | ✓ | GM が忖度しすぎる恐れ | ✓ |
| B：誰も読まない | ✗ | 最も TRPG らしい | ✗ |
| C：記録のみ（推奨） | ✗ | ゲーム進行に影響しない | ✓ |

**推奨は C**：内心は public ログにも他エージェントにも渡さず、
セッション記録ファイルにのみ書く。ゲームの情報分離と物語の読み応えを両立する。

### コンテキスト管理：差分送信で増大を抑える

永続エージェントへの送信は毎回「差分のみ」とする：

```
✗ 毎回フルログを送る（セッションが長くなるにつれ肥大）
✓ 前回送信以降の追加分だけを送る（サイズ一定）
  → エージェントの蓄積コンテキストがフルログの代わりを担う
```

GM は全情報を受け取るためコンテキストが最も早く増大する。
5〜10シーンごとに圧縮 SendMessage を挟む：
「直近2シーンを除いた過去を200トークン以内で要約してください」

---

## 比較表（改訂版）

| 観点 | 案1（全員逐次） | 案2（PC並列） | 案3（GM永続） | 案4（GMバッチ） | 案5（全員永続＋公開ログ）★ |
|------|---------------|--------------|--------------|----------------|--------------------------|
| GM の記憶 | ✗ 毎回再渡し | ✗ 毎回再渡し | ✓ 永続 | ✗ 毎回再渡し | ✓ 永続 |
| PC の記憶 | ✗ | ✗ | ✗ | ✗ | ✓ 永続 |
| PC の秘匿保証 | ✓ | ✓ | ✓ | ✓ | ✓ |
| PC の「同時思考」 | ✗ | ✓ | △ | △ | ✓ |
| 一貫性の担保方法 | 要約の精度 | 要約の精度 | GM 文脈 | 要約の精度 | 各エージェント文脈 |
| オーケストレーター負荷 | 低 | 中（情報配送） | 中（同期） | 低 | 低（書記のみ） |
| 実装の単純さ | ◎ | ○ | △ | ○ | ○ |
| セッション長期耐久性 | △ | △ | △（GM肥大） | △ | ○（差分送信で緩和） |

---

## 採用推奨（案5）

案2の実運用（session_log_01）で判明した根本問題は
「オーケストレーターが情報の仕分け・配送を担う設計」にあった。

案5はこの設計を転換する：

- **読み取りは放送**：公開ログを全員が差分で受け取る
- **書き込みは直列**：オーケストレーターのみが公開ログへ書く（競合なし）
- **一貫性はエージェントの文脈が担保**：要約の精度に依存しない
- **秘匿は「渡さない」だけ**：案2 の原則をそのまま維持

案3（GM永続）が「SendMessage の制御コストが高い」として却下された理由は、
PC がステートレスであることによる転送コストの高さにあった。
案5 では全員を永続化することでその問題を解消している。

---

## シーン内プロトコル詳細

案5 のターン進行をシーン内部まで掘り下げた設計。

### PCフェーズの構造：協議ラウンド制

PC 行動フェーズは「初期思考→協議→行動宣言」の三段階とする。
協議は複数ラウンドの繰り返しで、PC 間の議論・GM への質問・行動の再検討を許す。

```
GM描写
  │
  ▼
【初期思考フェーズ】（全PC並列・非公開）
  全PCが inner のみ返す。public 発言なし。

  │
  ▼
【協議フェーズ】（最大 N ラウンド）
  ┌─────────────────────────────────────────┐
  │  全PCが並列応答（前ラウンドの公開ログ差分を読んで）  │
  │    ↓                                         │
  │  query があれば GM へ転送 → GM が公開回答      │
  │    ↓                                         │
  │  public をログに追記 / inner・meta を記録へ    │
  │    ↓                                         │
  │  全員 status: ready? ─── No ────────────────┘
  │         ↓ Yes（または最大ラウンド到達）
  └─────────────────────────────────────────┘
  │
  ▼
【行動宣言フェーズ】（全PC並列）
  最終行動を宣言。

  │
  ▼
GM裁定 → 結果描写
```

### PC の出力フォーマット（協議ラウンド）

各ラウンドで PC エージェントが返す JSON：

```json
{
  "status": "speaking | ready",
  "speech": "「発言内容」または null",
  "action": "行動描写または null",
  "inner":  "（内心：……）",
  "meta":   "テストプレイ観点・ルール質問・プレイヤー意図",
  "query":  "GMへの確認事項または null"
}
```

| フィールド | 公開ログに書くか | 誰が読むか |
|-----------|----------------|-----------|
| `speech` / `action` | ✓ | 全員 |
| `inner` | ✗ | セッション記録のみ |
| `meta` | ✗ | セッション記録のみ（振り返りに活用） |
| `query` | GMが回答した形で ✓ | 全員（GMの回答として） |

### GM クエリの処理

PC が `query` を返した場合、オーケストレーターは同一ラウンド内で GM に転送する。
追加のラウンドは不要。

```
協議ラウンド N：
  全PCが並列応答
    └─ PC2: query: 「魔力感知式トラップの判定条件は？」
  ↓
  オーケストレーター：
    PC1・PC3 の speech をログに追記
    PC2 の query を GM へ SendMessage
    GM の回答をログに追記（PC2 の発言直後の位置に）
  ↓
  協議ラウンド N+1：全PCが回答を読んだ状態で始まる
```

### PC 間の依頼

特別な仕組みは不要。「ラット、扉を見てくれ」は通常の `speech` として公開ログに入る。
依頼された PC は次のラウンドでそれを読んで応答する。
依頼・受諾・再検討はすべて協議ラウンドの自然な繰り返しで実現する。

---

## プレイヤーメタ構造

### 設計の意図

TRPG では各プレイヤーが「キャラクター」と「プレイヤー」の二層を持つ。
プレイヤー層をエージェントに持たせることで：

- キャラクターが論理的に選ばない行動を「人間的な理由」で選べる
- テストプレイ観点のフィードバックをゲーム進行から分離できる
- キャラクター行動の一貫性と意外性のバランスが豊かになる

```
キャラクター行動原理だけでは：
  「ラットは慎重なので毎回罠を確認してから進む」→ 行動が予測可能になる

プレイヤー層が加わると：
  「プレイヤーが今日は迂回策を試したい」
  →「扉を外す」という予測外の発想が行動原理から生まれる

  「プレイヤーがカインとラットの関係を掘り下げたい」
  →「仕事として参加している」はずのラットが感情的な発言をするきっかけになる
```

### PC エージェントへの入力：二層のプロフィール

```markdown
## プレイヤープロフィール
プレイスタイル：慎重派。罠・謎解きに積極的に関与したい。
今セッションの目標：「罠感知→解除」の二段判定をテストしたい。
キャラクターへの関与度：冷静に操作している。感情移入は意識的に抑えている。

## キャラクターシート
名前：ラット
行動原理：「生きて報酬を持ち帰る。ただし仕事は丁寧にやる。」
能力値：【体】2【技】5【知】2【意】3
…
```

### 三層の出力とその用途

| レイヤー | フィールド | 用途 |
|----------|-----------|------|
| プレイヤー層 | `meta` | テストプレイ観点・ルール質問・プレイヤー意図。記録のみ、ゲーム進行に影響しない |
| キャラクター内面 | `inner` | キャラクターの思考・感情。記録のみ |
| キャラクター行動 | `speech` / `action` | 公開ログに書く。全員が読む |

`meta` の集積はセッション後の `【セッション後メモ】` 生成に使う。
GM が単独で書いていた `［システムメモ〕` を、全プレイヤー視点のフィードバックで補完できる。

### ≪原則≫ 記法との関係

既存の `≪原則：……≫` 記法は `meta` と `inner` の境界にある表現だった。
新しい設計では役割を分離する：

- `≪原則≫`：キャラクターがなぜその行動を選んだかの開示 → `inner` に含める
- プレイヤーがシステムやシナリオについて述べたいこと → `meta` に含める

---

## 比較表（改訂版）

| 観点 | 案1（全員逐次） | 案2（PC並列） | 案3（GM永続） | 案4（GMバッチ） | 案5（全員永続＋公開ログ）★ |
|------|---------------|--------------|--------------|----------------|--------------------------|
| GM の記憶 | ✗ 毎回再渡し | ✗ 毎回再渡し | ✓ 永続 | ✗ 毎回再渡し | ✓ 永続 |
| PC の記憶 | ✗ | ✗ | ✗ | ✗ | ✓ 永続 |
| PC の秘匿保証 | ✓ | ✓ | ✓ | ✓ | ✓ |
| PC の「同時思考」 | ✗ | ✓ | △ | △ | ✓ |
| PC 間の協議 | ✗ | △ 追加spawn必要 | △ | △ | ✓ ラウンド制 |
| GM への質問 | ✗ | ✗ | △ | △ | ✓ 同一ラウンド内 |
| プレイヤーメタ層 | ✗ | ✗ | ✗ | ✗ | ✓ |
| 一貫性の担保方法 | 要約の精度 | 要約の精度 | GM 文脈 | 要約の精度 | 各エージェント文脈 |
| オーケストレーター負荷 | 低 | 中（情報配送） | 中（同期） | 低 | 低（書記のみ） |
| 実装の単純さ | ◎ | ○ | △ | ○ | ○ |
| セッション長期耐久性 | △ | △ | △（GM肥大） | △ | ○（差分送信で緩和） |

*記録日：2026-03-27*
*改訂：2026-03-29　案5（全員永続＋公開ログ共有）を追加。案2の問題点を session_log_01 の実績を踏まえて記録。*
*改訂：2026-03-29 (2)　協議ラウンド制・GMクエリ処理・プレイヤーメタ構造を追加。比較表を更新。*
